function openEvent(id){ state.selectedEventId=id; const ev=getEvent(id); state.selectedTicket=ev.ticketTypes[0].name; state.selectedPrice=ev.ticketTypes[0].price; state.favoritePerformer=ev.performers[0] || ''; go('/event/'+id); }
function shareOnX(id){ const ev = getEvent(id); const text = encodeURIComponent(`${ev.title} を Live Gate でチェック！ ${ev.performers.join(' / ')} #LiveGate`); const url = encodeURIComponent(location.href.split('#')[0] + '#/event/' + ev.id); window.open(`https://twitter.com/intent/tweet?text=${text}&url=${url}`,'_blank'); toast('X共有画面を開きました'); }
function showOrganizerInfo(id){ const org = getEvent(id).organizer; openModal(`<span class="eyebrow">ORGANIZER</span><h2>${org.name}</h2><div class="section-sub">担当：${org.manager}</div><div class="organizer-box" style="margin-top:16px"><div><b>X</b><div>${org.x}</div></div><div><b>メール</b><div>${org.email}</div></div><div><b>主催者コメント</b><div>${org.note}</div></div></div>`,[{label:'閉じる',type:'soft'}]); }
function selectFavorite(name){ state.favoritePerformer = name; persistFirebasePreference('favoritePerformer', name); render(); toast(`お目当て演者を「${name}」に設定しました`); }
function selectTicket(name,price){ state.selectedTicket=name; state.selectedPrice=price; render(); }
function detail(){
  const ev = currentEvent();
  return `<div class="user-shell">${userHeader()}<main class="user-main"><div class="event-detail"><button class="btn btn-soft" style="margin-bottom:16px" onclick="go('/')">← イベント一覧</button><div class="detail-hero"><span class="chip ${ev.badgeClass}">${ev.statusLabel}</span></div><section class="detail-card"><span class="eyebrow">LIVE</span><h1>${ev.title}</h1><p class="section-sub">${ev.performers.join(' / ')}</p><div class="detail-meta"><div class="info-tile"><b>◷ ${ev.date}</b><span>OPEN ${ev.open} / START ${ev.start}</span></div><div class="info-tile"><b>⌖ ${ev.venue}</b><span>${ev.area}</span></div><div class="info-tile"><b>販売状況</b><span>${ev.statusLabel}</span></div></div><div class="action-row"><button class="btn btn-soft" onclick="shareOnX('${ev.id}')">𝕏 で共有</button><button class="btn btn-soft" onclick="showOrganizerInfo('${ev.id}')">主催者情報</button><button class="btn btn-soft" onclick="toggleLike('${ev.id}')">${state.likedEvents[ev.id]?'♥ お気に入り済み':'♡ お気に入り'}</button></div><h2 style="font-size:20px;margin-top:28px">イベントについて</h2><p class="section-sub">${ev.description}</p><h2 style="font-size:20px;margin-top:28px">出演者</h2><div class="performer-grid">${ev.performers.map(name=>`<div class="performer-card ${state.favoritePerformer===name?'active':''}"><div><b>${name}</b><div class="mini">出演者</div></div><button class="btn ${state.favoritePerformer===name?'btn-primary':'btn-soft'}" style="min-height:38px" onclick="selectFavorite('${name}')">${state.favoritePerformer===name?'選択中':'お目当てにする'}</button></div>`).join('')}</div><h2 style="font-size:20px;margin-top:28px">主催者情報</h2><div class="organizer-box"><div><b>${ev.organizer.name}</b></div><div class="section-sub">担当：${ev.organizer.manager}</div><div class="section-sub">X：${ev.organizer.x} / Mail：${ev.organizer.email}</div><button class="btn btn-soft" style="width:max-content" onclick="showOrganizerInfo('${ev.id}')">詳細を見る</button></div><h2 style="font-size:20px;margin-top:28px">チケットを選ぶ</h2><div class="ticket-list">${ev.ticketTypes.map(t=>`<div class="ticket-option ${state.selectedTicket===t.name?'selected':''}" onclick="selectTicket('${t.name}',${t.price})"><div><h3>${t.name}</h3><p>${t.note} ・ ${t.stock}</p></div><div><div class="price">${money(t.price)}</div><small class="chip ${t.status==='残りわずか'?'chip-orange': t.status==='販売前'?'chip-blue':'chip-green'}">${t.status}</small></div></div>`).join('')}</div></section><div class="sticky-purchase"><div class="sum"><small>選択中</small><b>${state.selectedTicket} · ${money(state.selectedPrice)} / お目当て：${state.favoritePerformer || '未選択'}</b></div><div style="display:flex;gap:10px;flex-wrap:wrap"><button class="btn btn-soft" onclick="shareOnX('${ev.id}')">𝕏で共有</button><button class="btn btn-primary" onclick="go('/checkout')">チケットを購入</button></div></div></div></main>${userFooter()}${bottomNav('home')}</div>`;
}
function changeQty(diff){ state.qty = Math.min(4, Math.max(1, state.qty + diff)); render(); }
function setFavoriteFromCheckout(value){ state.favoritePerformer = value; persistFirebasePreference('favoritePerformer', value); render(); }
function purchase(){ localStorage.setItem('livegate-demo-ticket','yes'); toast('購入が完了しました'); setTimeout(()=>go('/tickets'),500); }
function checkout(){
  const ev = currentEvent();
  const total = state.selectedPrice * state.qty + state.fee;
  return `<div class="user-shell">${userHeader()}<main class="user-main"><div class="checkout-wrap"><section class="panel"><div class="stepper"><span class="step"><i>1</i>選択</span><span class="step-line"></span><span class="step active"><i>2</i>確認・決済</span><span class="step-line"></span><span class="step"><i>3</i>完了</span></div><h2>購入者情報</h2><div class="field"><label>お名前</label><input value="山田 太郎" /></div><div class="field"><label>メールアドレス</label><input value="taro@example.com" /></div><div class="field"><label>お目当て演者</label><select onchange="setFavoriteFromCheckout(this.value)">${ev.performers.map(name=>`<option ${state.favoritePerformer===name?'selected':''}>${name}</option>`).join('')}</select></div><div class="field"><label>メッセージ（任意）</label><textarea placeholder="主催者への連絡事項">楽しみにしています！</textarea></div><h2 style="margin-top:26px">お支払い方法</h2><div class="field"><label>カード番号</label><input value="4242 4242 4242 4242" /></div><div style="display:grid;grid-template-columns:1fr 1fr;gap:10px"><div class="field"><label>有効期限</label><input value="12/29" /></div><div class="field"><label>セキュリティコード</label><input value="123" /></div></div><div class="trust">🔒 この画面はブラッシュアップ版のUIプロトタイプです。実決済は行われません。</div></section><aside class="panel"><h2>注文内容の最終確認</h2><div style="display:flex;gap:12px;align-items:center;padding-bottom:15px;border-bottom:1px solid var(--line)"><div class="thumb" style="width:76px;height:52px"></div><div><b>${ev.title}</b><div class="section-sub">${ev.date} ${ev.start} · ${ev.venue}</div></div></div><div class="summary-line"><span>${state.selectedTicket}</span><span class="qty"><button onclick="changeQty(-1)">−</button><b>${state.qty}枚</b><button onclick="changeQty(1)">＋</button></span></div><div class="summary-line"><span>お目当て演者</span><span>${state.favoritePerformer || '未選択'}</span></div><div class="summary-line"><span>チケット料金</span><span>${money(state.selectedPrice * state.qty)}</span></div><div class="summary-line"><span>システム利用料</span><span>${money(state.fee)}</span></div><div class="summary-line total"><span>合計金額</span><b>${money(total)}</b></div><p class="section-sub">購入後のキャンセルは主催者規定に従います。確定前に内容をご確認ください。</p><div class="action-row" style="margin-top:12px"><button class="btn btn-primary" style="flex:1" onclick="purchase()">${money(total)}を支払って購入する</button><button class="btn btn-soft" style="flex:1" onclick="go('/event/${ev.id}')">内容を修正する</button></div><button class="btn btn-soft" style="width:100%;margin-top:8px" onclick="shareOnX('${ev.id}')">𝕏で共有して友達を誘う</button></aside></div></main>${bottomNav('home')}</div>`;
}
function qrHtml(){
  const arr=[];
  for(let y=0;y<21;y++){
    for(let x=0;x<21;x++){
      const finder=(x<7&&y<7)||(x>13&&y<7)||(x<7&&y>13);
      const ring=finder&&((x%14===0)||(y%14===0)||(x%14===6)||(y%14===6));
      const center=finder&&(x%14>=2&&x%14<=4&&y%14>=2&&y%14<=4);
      const on=ring||center||((x*17+y*31+x*y*3)%7<3&&!finder);
      arr.push(`<span class="${on?'on':''}"></span>`);
    }
  }
  return arr.join('');
}
function purchasedTickets(){
  return [
    {eventId:'summer-live-2026', status:'before', count:'あと17日', serial:'A-042', qty:2},
    {eventId:'jazz-night-2026', status:'ended', count:'開催終了', serial:'C-013', qty:1}
  ];
}
function ticketFilterMatch(ticket){
  if(state.ticketTab==='all') return true;
  return ticket.status === state.ticketTab;
}
function tickets(){
  const items = purchasedTickets().filter(ticketFilterMatch);
  return `<div class="user-shell">${userHeader()}<main class="user-main"><div class="ticket-page"><span class="eyebrow">MY TICKETS</span><h1 style="font-size:32px;letter-spacing:-.04em;margin:5px 0 18px">マイチケット</h1><div class="tabs"><button class="tab ${state.ticketTab==='all'?'active':''}" onclick="setTicketsTab('all')">すべて</button><button class="tab ${state.ticketTab==='before'?'active':''}" onclick="setTicketsTab('before')">開催前</button><button class="tab ${state.ticketTab==='live'?'active':''}" onclick="setTicketsTab('live')">開催中</button><button class="tab ${state.ticketTab==='ended'?'active':''}" onclick="setTicketsTab('ended')">終了</button></div>${items.length ? items.map(ticket => { const ev=getEvent(ticket.eventId); return `<article class="my-ticket"><div class="ticket-cover"><div class="countdown">${ticket.count}</div><h2>${ev.title}</h2><div style="font-size:12px;color:#d9d7e6">${ev.date} ${ev.start} · ${ev.venue}</div></div><div class="ticket-body"><div class="chip ${ev.badgeClass}">${state.selectedTicket || '一般チケット'}</div><div class="ticket-code"><div class="serial"><small>整理番号</small><strong>${ticket.serial}</strong><small>${ticket.qty}枚</small></div><div class="qr">${qrHtml()}</div></div><p style="text-align:center;color:var(--pink);font-size:13px;font-weight:900">🔒 入場時にスタッフへこの画面を提示してください</p><div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:16px"><button class="btn btn-soft" onclick="toast('画面の明るさを上げてください')">☀ 画面を明るくする</button><button class="btn btn-soft" onclick="showOrganizerInfo('${ev.id}')">主催者情報</button><button class="btn btn-soft" onclick="shareOnX('${ev.id}')">𝕏で共有</button><button class="btn btn-soft" onclick="openEvent('${ev.id}')">イベント詳細</button></div></div></article>`; }).join('') : `<div class="empty"><div class="empty-art"></div><b>表示できるチケットはありません</b><small>タブを切り替えるか、イベントを購入してください。</small><div style="margin-top:16px"><button class="btn btn-primary" onclick="go('/')">イベントを探す</button></div></div>`}</div></main>${userFooter()}${bottomNav('ticket')}</div>`;
}
function profile(){
  return `<div class="user-shell">${userHeader()}<main class="user-main"><div class="ticket-page"><span class="eyebrow">PROFILE</span><h1 style="font-size:32px;letter-spacing:-.04em;margin:5px 0 18px">プロフィール</h1><div class="panel"><div style="display:flex;align-items:center;gap:16px;margin-bottom:12px"><div class="avatar" style="width:64px;height:64px">山</div><div><b>山田 太郎</b><div class="section-sub">taro@example.com</div></div></div><div class="participant-list"><button class="participant-item" onclick="go('/tickets')"><div><b>購入履歴</b><div class="mini">チケット管理へ移動</div></div><span>→</span></button><button class="participant-item" onclick="toast('お気に入り公演を表示しました')"><div><b>お気に入りイベント</b><div class="mini">${Object.values(state.likedEvents).filter(Boolean).length}件</div></div><span>→</span></button><button class="participant-item" onclick="toast('支払い方法の管理を表示しました')"><div><b>支払い方法</b><div class="mini">Visa **** 4242</div></div><span>→</span></button></div></div></div></main>${userFooter()}${bottomNav('ticket')}</div>`;
}
