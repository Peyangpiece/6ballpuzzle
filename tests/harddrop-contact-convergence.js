const {runSuite}=require('./audit-harness');
const suite=String.raw`
const failures=[];function fail(type,data){failures.push({type,...data});}
const mk=(id,c)=>({id,c,motionGroupId:0,motionGroupRole:-1,motionGroupOrientation:'',motionGroupSize:0,rigid:false,momentumX:0,rollDir:0,subCellBias:0});
function visuals(g){const a=[];for(let y=boardScanMin(g.board);y<ROWS;y++)for(let x=0;x<W2;x++){const b=valid(x,y)?g.board[y][x]:null,v=b&&g.vis.get(b.id);if(b&&v)a.push({b,v,x,y});}return a;}
function holds(g){let n=0;for(const q of visuals(g))if(q.b.hardDropContactHold)n++;return n;}
function seedPile(g,seed,visibleOnly){const rng=mulberry32(910000+seed);let id=3000000+seed*60;for(let n=0;n<3+(seed%15);n++){const y=visibleOnly?Math.floor(rng()*ROWS):BOARD_MIN_ROW+Math.floor(rng()*(ROWS-BOARD_MIN_ROW)),xs=[];for(let x=0;x<W2;x++)if(valid(x,y)&&!g.board[y][x])xs.push(x);if(!xs.length)continue;const x=xs[Math.floor(rng()*xs.length)],c=Math.floor(rng()*COLORS.length);g.board[y][x]=mk(id++,c);}settleAll(g.board);try{Object.defineProperty(g.board,'_hexEngine',{value:g,writable:true,configurable:true,enumerable:false});}catch(_){}for(let y=boardScanMin(g.board);y<ROWS;y++)for(let x=0;x<W2;x++){const b=valid(x,y)?g.board[y][x]:null;if(b){delete b.fallPath;setVis(g,b,x,y,0);}}}
function arm(g,seed){g.state='PLAYING';g.piece.rot=seed%6;const range=legalXRange(g),f=((seed%31)+.37)/31;setFreeX(g,range[0]+(range[1]-range[0])*f);updateVisuals(g,PHYSICS_FRAME);const shadow=landingShadowVisualCells(g);if(!shadow)return null;const before=g.nextId;hardDrop(g);return{shadow,before,tracked:new Set([before,before+1,before+2])};}
function motionMeta(b){const s=b?.fallPath?.[0];return s?{from:s.from,to:s.to,kind:s.kind,arc:s._hexHardDropRigidArc||null,pathN:b.fallPath.length,protect:b._hexHardDropProtectRigid||0,gid:b.motionGroupId||0,rigid:!!b.rigid}:null;}
function checkFrame(g,seed,step,tracked,prevY){const arr=visuals(g);for(const q of arr){if(!Number.isFinite(q.v.x)||!Number.isFinite(q.v.y)){fail('nonfinite-visual',{seed,step,id:q.b.id});continue;}if(tracked.has(q.b.id)){const py=prevY.get(q.b.id);if(Number.isFinite(py)&&q.v.y<py-1e-6)fail('tracked-upward-motion',{seed,step,id:q.b.id,from:py,to:q.v.y,visual:[q.v.x,q.v.y],motion:motionMeta(q.b)});prevY.set(q.b.id,q.v.y);}}for(let i=0;i<arr.length;i++)for(let j=i+1;j<arr.length;j++){const d=hexPhysDist(arr[i].v.x,arr[i].v.y,arr[j].v.x,arr[j].v.y);if(d<0.9999)fail('visual-overlap',{seed,step,d,a:arr[i].b.id,b:arr[j].b.id,state:g.state,phase:g.phase});}}
// Exact landing-smoke fixtures: validate the first visible contact continuation.
for(const seed of [1,3,5,6,9]){const g=createEngine(710000+seed);spawn(g);seedPile(g,seed,false);const a=arm(g,seed);if(!a){fail('missing-shadow-exact',{seed});continue;}const prevY=new Map();for(let step=0;step<30&&g.alive;step++){checkFrame(g,seed,step,a.tracked,prevY);stepEngine(g,PHYSICS_FRAME);}}
// Visible-only piles: validate long convergence without an unrelated overflow GAMEOVER.
for(const seed of [1,3,5,6,9,11,17,23]){const g=createEngine(810000+seed);spawn(g);seedPile(g,seed,true);const a=arm(g,seed);if(!a){fail('missing-shadow-visible',{seed});continue;}const prevY=new Map();let settled=false;for(let step=0;step<600&&g.alive;step++){checkFrame(g,seed,step,a.tracked,prevY);if(g.state!=='RESOLVING'){settled=true;break;}stepEngine(g,PHYSICS_FRAME);}if(!g.alive)fail('visible-fixture-gameover',{seed,reason:g.gameOverReason});if(holds(g)>0)fail('harddrop-hold-stuck',{seed,holds:holds(g),state:g.state,phase:g.phase});if(!settled&&g.state==='RESOLVING')fail('harddrop-resolution-stuck',{seed,phase:g.phase,holds:holds(g)});}
globalThis.__HARD_DROP_CONVERGENCE={failures};
`;
const ctx=runSuite(suite,{timeout:180000});console.log('HARD_DROP_CONVERGENCE',JSON.stringify(ctx.__HARD_DROP_CONVERGENCE,null,2));if(ctx.__HARD_DROP_CONVERGENCE.failures.length)process.exitCode=1;
