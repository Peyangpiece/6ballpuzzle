name: Legacy Parity Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Explain archived diagnostic
        shell: bash
        run: |
          echo 'This legacy diagnostic depended on public/app-wall-gap-invariant.js, which is no longer part of the authoritative production runtime.'
          echo 'Current split/release behavior is validated by the V1303 oracle and up-convex production guards.'
