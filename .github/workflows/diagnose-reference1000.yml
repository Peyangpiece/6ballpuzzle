name: Legacy Reference 1000

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Explain archived diagnostic
        shell: bash
        run: |
          echo 'This legacy 1000-point harness depended on public/app-pileflow-visual-tangency.js, which has been removed from the authoritative runtime.'
          echo 'Reference fidelity is now guarded by tests/v1303-current-oracle-suite.js with 10,000 random plus 2,000 directed cases.'
