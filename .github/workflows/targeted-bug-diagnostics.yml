name: Targeted HEXDROP Bug Diagnostics

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  targeted:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node tests/audit-targeted-first-failures.js
      - run: node tests/seed7-step3414-parallel-pile-contact.js
      - name: Capture targeted traces
        run: |
          mkdir -p diagnostics
          node tests/seed7-step3414-write-trace.js > diagnostics/seed7-step3414-write-trace.log
          node tests/seed7-step3414-contact-pass-trace.js > diagnostics/seed7-step3414-contact-pass-trace.log
      - name: Persist traces
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git pull --rebase origin main
          git add diagnostics/seed7-step3414-write-trace.log diagnostics/seed7-step3414-contact-pass-trace.log
          if ! git diff --cached --quiet; then
            git commit -m "Store seed7 contact traces [skip ci]"
            git push
          fi
