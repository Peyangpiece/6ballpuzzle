name: Legacy Clear Vacancy Priority

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Explain archived diagnostic
        shell: bash
        run: |
          echo 'This legacy diagnostic depended on public/app-wall-gap-invariant.js, which is no longer part of the authoritative production runtime.'
          echo 'Post-clear behavior is validated by the current V1303 production gate and current garbage-physics audit.'
